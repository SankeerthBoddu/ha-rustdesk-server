---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    # Weekly run is sufficient for low-volume repos and reduces Actions noise.
    - cron: "0 9 * * 0"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    name: Lock closed issues and PRs
    runs-on: ubuntu-latest
    steps:
      - uses: dessant/lock-threads@v5
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          issue-inactive-days: "30"
          pr-inactive-days: "30"
